//we import two modules, with each one representing just a single text
const module1 = require('./module1');
const module2 = require('./module2');

//we output them in console log
console.log(`${module1} ${module2}`);